{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser-shell",
  "description": "Only the trusted Ghost Browser chrome receives native window/IPC permissions. Remote tab webviews are intentionally excluded.",
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close"
  ]
}
